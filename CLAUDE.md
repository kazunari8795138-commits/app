# このリポジトリについて

診断アプリを複数まとめたリポジトリ。各アプリは独立したディレクトリに置き、
`index.html` / `style.css` / `data.js` / `app.js` の4ファイル構成で作る。
既存アプリを壊さず、新しい診断は新しいディレクトリに追加していく。

## ルート直下: 自己否定パターン診断

`index.html` / `app.js` / `data.js` / `style.css`（ルート直下）。
YES/NO・18問（6タイプ×3問）。タイプ診断（同点時は最大2問のタイブレークを追加出題）。

## `/divorce/`: 夫婦関係の危機度セルフチェック（離婚リスク診断）

自己否定パターン診断と同じ構成・作法で作った新アプリ。

- 24問（8カテゴリ×3問）のYES/NO診断
- カテゴリ: 会話すれ違い / 価値観の不一致 / 金銭感覚 / 精神的すれ違い / 信頼・異性関係 / スキンシップ不足 / 家事育児分担 / 親族関係
- 質問設計は司法統計上の離婚理由（性格の不一致1位、精神的虐待・生活費・異性関係など）や関連コラムを参考にした
  （参考記事 https://www.daylight-law.jp/divorce/column/column14/ は本環境からの直接アクセスがネットワーク制限でブロックされたため、WebSearchで内容を補完調査した）
- 結果は「危機度%」をリング型チャートで表示（タイプ診断ではなくスコア/百分率で結果を出す設計）
- **安全設計**: 「精神的すれ違い」カテゴリに該当（YES）が1つでもあると、スコアの高低に関わらずDV相談窓口（DV相談ナビ #8008、DV相談＋、法テラス）を案内するバナーを表示する。この設計は削除・弱体化しない。
- タイトル画面にハート×虫眼鏡のシンプルなSVGイラストあり
- 免責文で「科学的予測ではなく自己理解のためのセルフチェック」であることを明記している。この文言のトーンは維持すること

## 動作確認の方法

このリポジトリはビルド不要の静的サイト（フレームワーク・パッケージ管理なし）。
変更後は `python3 -m http.server` 等でローカル配信し、Playwright
（`/opt/pw-browsers/chromium`、`NODE_PATH=/opt/node22/lib/node_modules node ...`）で
一連の画面遷移をライト/ダーク両方スクリーンショット確認してから完了とする。

## ユーザーへのテスト提供

ユーザーが「テスト画面」「DLしたい」と言った場合の対応:
- **試したいだけ**: 対象アプリの `index.html`/`style.css`/`data.js`/`app.js` を1ファイルに
  インライン結合（`<link>`→`<style>`、2つの`<script src>`→1つの`<script>`）し、Artifactとして公開してURLを渡す。
- **保存・オフライン利用したい**: 同様に1ファイル化した完全なHTML文書（`<!doctype html>`から
  `</html>`まで、外部参照なし）を作り、`SendUserFile`で送る。
- どちらも都度その場で生成する一時ファイルであり、リポジトリにはコミットしない。

## 進行の合意事項

- PRは明示的に頼まれない限り作成しない（現状: 作成しないでよいと言われている）。
- 新しい診断アプリを追加するときは、既存アプリのファイルを上書きせず、新規ディレクトリに追加する。
